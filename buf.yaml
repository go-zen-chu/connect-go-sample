# buf.yamlファイルは、Bufプロジェクトの設定を定義するために使用されます。
# これには、プロジェクトに含まれる.protoファイルの場所、各ファイルのオプション、および生成されるコードの言語やパスなどが含まれます。
# これは、Bufコンパイラによって直接処理されるファイルです。
version: v1
breaking:
  use:
    - FILE
lint:
  use:
    - DEFAULT
